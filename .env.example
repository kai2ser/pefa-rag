# Anthropic Claude (chat completions)
ANTHROPIC_API_KEY=

# OpenAI (text-embedding-3-large, 3072-dim embeddings)
OPENAI_API_KEY=

# Supabase project (reuse the pim-guidance project so the existing
# documents/collections tables are shared across collections)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
